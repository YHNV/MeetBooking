<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- <mapper namespace="com.example.mapper.UserMapper"> -->

<mapper namespace="com.zb.backend.mapper.AnnouncementMapper">
    <!-- Announcement实体类与announcements表的字段映射 -->
    <resultMap id="AnnouncementResultMap" type="com.zb.backend.entity.Announcement">
        <result property="announcementId" column="announcement_id"/>
        <result property="senderId" column="sender_id"/>
        <result property="title" column="title"/>
        <result property="content" column="content"/>
        <result property="isActive" column="is_active"/>
        <result property="createTime" column="create_time"/>
        <result property="updateTime" column="update_time"/>
    </resultMap>
    <insert id="insertAnn">
        insert into ANNOUNCEMENTS (sender_id, title, content)
        values (#{senderId}, #{title}, #{content});
    </insert>
    <update id="updateAnn">
        update ANNOUNCEMENTS
        set TITLE = #{title}, CONTENT = #{content}, IS_ACTIVE = #{isActive}
        where ANNOUNCEMENT_ID = #{announcementId};
    </update>
    <delete id="deleteAnn">
        delete
        from ANNOUNCEMENTS
        where ANNOUNCEMENT_ID = #{announcementId};
    </delete>
    <select id="countAnn" resultType="java.lang.Integer">
        select count(*)
        from ANNOUNCEMENTS;
    </select>
    <select id="selectAnnList" resultType="com.zb.backend.entity.Announcement">
        select announcement_id, sender_id, title, content, is_active, create_time, update_time
        from ANNOUNCEMENTS
        where 1=1
        <if test="pageNum == null">

        </if>
        ORDER BY CREATE_TIME DESC
        <!-- 关键：使用 ${} 直接拼接，因为 MyBatis 对 LIMIT 子句中的 #{} 处理在不同数据库下可能有问题 -->
        LIMIT ${(pageNum - 1) * pageSize}, #{pageSize}
    </select>
    <select id="selectById" resultType="com.zb.backend.entity.Announcement">
        select *
        from ANNOUNCEMENTS
        where ANNOUNCEMENT_ID = #{announcementId};
    </select>
</mapper>